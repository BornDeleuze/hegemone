<h2>Here's your plants!</h2>

<% user = current_user%>
<% if user.plants[0] != nil %>
        <ul>
        <% user.plants.each do |plant|%>
            <li>
            <a href="/plants/<%= plant.id %>" >
            <%= plant.name %> </a>
            </li>
        <% end %>
        </ul>
    <% else %>
    <h3>You don't have any plants!?</h3>
    
<% end %>
<br>
<a href="/plants/new" > Add a new plant here!</a>
<br><br>
<h2>Here's your gardens!</h2>
<ul>
<% array = []%>
<% user.plants.each do |plant|%>
        <% if array.include?(plant.garden_name)%>
        <% else %>
            <% array << plant.garden_name%>
            <li><a href="/gardens/<%= plant.garden_name %>" >
            <%= plant.garden_name %> </a> </li>    
        <% end %>
    <% end %>

</ul>